---
layout: default
---

<h2><%= @document.title %></h2>
<% if @document.coverImage?: %>
  <img src="<%= @document.coverImage %>" alt="Image of the front cover of <%= @document.title %>">
<% end %>
<%- @content %>
